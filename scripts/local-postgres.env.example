# Local Postgres credentials for bootstrap-dev-database.sh --local-postgres
# Copy to scripts/local-postgres.env and adjust if needed (file is gitignored).
STARDESK_LOCAL_PG_USER=stardesk
STARDESK_LOCAL_PG_PASSWORD=stardesk_dev
