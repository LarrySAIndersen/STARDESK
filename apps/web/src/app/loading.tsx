import { AgentMainLoading } from "@/components/agent/agent-main-loading";

/** Route-level loading UI while server components fetch data. */
export default function Loading() {
  return <AgentMainLoading />;
}
