# STARdesk Web — PRODUCTION (Vercel project "web")
# Set in Vercel → Production. Never commit secrets.

NEXT_PUBLIC_API_URL=https://api-gamma-amber.vercel.app

# Hide demo passwords on login in hardened prod
NEXT_PUBLIC_ENABLE_DEMO_LOGIN=false

# Omit both BASIC_AUTH_* in production unless you intentionally gate the site
