# Fallback when Dockerfile is not selected (Nixpacks auto-detect).
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install ."]

[start]
cmd = "uvicorn star_itsm_api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
