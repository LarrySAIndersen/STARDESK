-- Fuld kategoriliste + underkategorier (idempotent).
-- Kør på eksisterende DB, eller brug POST /api/v1/admin/categories/sync-defaults fra admin UI.

-- Underkategorier for alle kategorier oprettes via seed-mvp.sql (general) eller sync-defaults API.
